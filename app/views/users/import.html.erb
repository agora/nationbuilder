<div id="hp_intro"><%= t("users.import.title") %></div>

<% form_tag(import_users_path) do -%>
  <%= hidden_field_tag("authenticity_token", form_authenticity_token) %>
  <%= text_area_tag("userlist", "", :rows => 25) %>
  <%= submit_tag(t("users.import.submit")) %>
<% end %>